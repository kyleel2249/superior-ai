# Wave 14 — Semantic pack search

- Embeddings when provider key set; lexical fallback always
- Hybrid score + marketplace rank blend
- GET /api/packs?q=... (&ranked=1)
- POST search | embed_catalog
- Admin UI search box
