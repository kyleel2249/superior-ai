# Wave 12

## Publisher portal
- /publisher UI + /api/publishers
- Register, publish signed pack, revenue share

## Code execution
- Validate-only by default
- ALLOW_CODE_EXEC=1 process sandbox with blocked patterns
- POST /api/exec

## Revenue share
- shareBps (default 70% publisher)
- Accrue on pack sales
