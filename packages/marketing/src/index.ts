/**
 * @superior-ai/marketing
 *
 * package.json declared "main": "./src/index.ts" but src/ was completely
 * empty and nothing in the repo imports this package yet. Placeholder so
 * the declared entry point actually exists; design its real API against
 * its first real consumer rather than guessing one here.
 */
export {};
