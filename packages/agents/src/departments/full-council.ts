/**
 * Re-export of the department/org-chart helpers implemented in ../index.ts.
 * (Previously this pointed at a source file that didn't exist in the repo —
 * see the note at the top of ../index.ts.)
 */
export { ALL_DEPARTMENTS, selectAgentsForGrowthTask, buildCompanyOrgChart } from "../index";
